# Codex Doctor V1

Initial Windows proxy auto-fix edition.

- Scans common local proxy ports.
- Creates or updates `%USERPROFILE%\.codex\.env`.
- Writes `HTTP_PROXY` and `HTTPS_PROXY`.
- Backs up an existing `.env`.
- Writes user-level proxy environment variables.
