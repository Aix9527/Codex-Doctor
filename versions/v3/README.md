# Codex Doctor V3 — Smart Proxy Edition

Adds Clash Verge / Mihomo configuration discovery, `mixed-port` / `port` / `socks-port` parsing and actual HTTPS proxy validation before writing Codex proxy settings.
