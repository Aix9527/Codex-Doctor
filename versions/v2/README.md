# Codex Doctor V2

Windows Codex migration + proxy repair edition. Adds diagnostics, `.codex` migration with NTFS Junction, backup/restore, Windows system proxy detection and optional `codex doctor` execution.
