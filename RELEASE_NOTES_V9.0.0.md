# Codex Doctor V9.0.0 Functional Rebuild

**软件作者：Aix**

V9.0.0 是一次**独立功能重构版（Functional Rebuild）**。V9 不再依赖 V8 程序集，不加载旧版主界面，也不通过反射挂载旧功能入口。它保留 V8.2.0 已验证的 Reconnecting 专项自愈理念，重新设计核心执行链路，以“明确故障范围、保留用户数据、提供可审计修复、不伪报成功”作为设计原则。

```text
扫描 / 证据收集
→ 问题分类
→ 生成修复计划
→ 备份
→ 执行
→ 验证
→ 失败回滚
→ 独立复检
→ 输出报告
```

## V8 → V9 架构变化

### 独立代码体系

- 不引用 V8 程序集；
- 不加载 V8 主窗体；
- 不通过反射挂载旧按钮；
- 使用独立功能模块 `versions/v9`。

### 修复流程重构

由“检测问题 → 执行修复”升级为证据化闭环：检测证据 → 生成修复计划 → 验证计划有效性 → 执行动作 → 验证结果 → 必要时恢复。

### 配置事务化

所有关键配置修改：

- 修改前生成唯一备份；
- 写入后重新读取验证；
- 验证失败自动恢复；
- 用户再次修改过文件时拒绝覆盖并提示手动恢复；
- 保留未知字段与自定义 `NO_PROXY`。

## V9.0.0 新功能

### 一键修复重连

新增“一键修复重连”入口，无需事先扫描、不依赖固定端口列表：

```text
发现本机候选代理
→ HTTPS 验证
→ 判断有效代理
→ 备份 .codex/.env
→ 更新 HTTP_PROXY / HTTPS_PROXY（含小写形式）
→ 保留其它配置
→ 重新读取复检
→ 输出结果
```

支持无凭据本机 HTTP / mixed 混合端口与 IPv6 loopback。

安全规则：**未验证代理不会写入；直连正常不会强制写代理；不修改 Windows 用户级代理。** 旧的失效代理配置不会阻止继续搜索新端口。

### 代理自动发现

综合 Windows 代理信息、进程代理环境、历史配置与本地监听端口发现候选代理，全部候选均需通过实际 HTTPS 验证。

### .codex/.env 安全修复

支持创建配置、更新配置、保留未知字段与 `NO_PROXY`、生成备份、失败恢复。

### Desktop / CLI 管理

支持客户端身份区分、启动 / 重启（正常退出后携代理启动）、官方 winget / npm 安装与卸载管理、CLI PATH 修复、配置只读属性修复。

### Git / npm 代理清理

检测代理冲突，备份默认用户配置后仅删除代理项，独立读取验证，失败恢复。

### 数据迁移与恢复

支持 `.codex` 数据迁移、Junction 创建与恢复、目标路径校验、中断事务恢复、撤销上次代理修复。

### 中文 / English 自动化

语言切换改用强类型 .NET Windows UI Automation：自动打开客户端设置、切换语言、验证实际结果；控件不兼容时明确标记未完成，不伪造语言配置。

### 报告导出

导出检查结果、执行记录、代理证据与备份位置；不导出配置原文或 API Key / Token / Cookie / 密码等凭据。

## 安全设计

- 默认不修改 Windows 全局代理；
- 未通过 HTTPS 验证的代理不会写入配置；
- 每个自动修复动作必须：有明确原因、有执行证据、有验证结果；
- 不读取 / 输出 API Key、Token、Cookie、账号密码；不上传用户配置；
- 无法确认的问题保持 `ManualRequired` 等真实状态，不会显示“已修复”。

## 测试范围

V9.0.0 已完成并在仓库留档（`docs/V9-测试与验收记录.md`、`versions/v9/VERIFICATION.md`）：

- **行为测试**：31 项全部通过，覆盖真实文件改写 / 备份 / 回滚、重复执行幂等、验证失败回滚、取消后恢复、真实 Git 配置清理、进程超时、真实 Junction 迁移与恢复、错误链接拒绝等；
- **真实代理集成测试**：真实检测并选中 `http://127.0.0.1:7897`，仅写入临时测试目录；
- **窗口交互测试**：专用 WinForms 设置窗口夹具上实际选中简体中文并验证、再选中 English 并验证；
- **发布验证**：`win-x64` 自包含单文件、EXE `--self-test` 通过、发布目录单 EXE 断言、SHA256 生成。

## 已知限制（未验证项）

V9.0.0 不声明以下能力已经过用户实机验收：

1. **真实机器恢复**：尚未用交付程序修改用户真实 `.codex/.env`，未重启用户正在使用的 Codex，未验证真实会话恢复；
2. **真实安装 / 卸载**：逻辑已实现，但未在用户机器替代验收，不代表所有客户端版本均通过；
3. **Reconnecting 根因范围**：可修复本地代理配置、`.env` 配置、本地连接路径问题；登录失效、服务端异常、额度限制、账号状态等仍需单独判断；
4. **Desktop 版本兼容**：不同 Desktop 版本在 UI 控件、设置入口、语言位置可能存在差异，V9 会记录“未完成”而不会伪造“成功”。

## 发布与验证

正式发布资产固定为：

```text
CodexDoctorV9.exe
CodexDoctorV9.exe.sha256
Codex-Doctor-V9-Source.zip
V9-使用说明.md
V9-测试与验收记录.md
```

构建目标：`.NET 8 / win-x64 / self-contained / single-file`。

V9.0.0 Release 门禁验证：

- V9 行为测试（31 项）与窗口交互测试全部通过；
- `CodexDoctorV9.exe` 具有 PE `MZ` 文件头；
- manifest 保持 `requireAdministrator`；
- 项目版本固定为 `9.0.0`；
- 发布目录无 `.ps1`、`.psm1`、外置 `.dll`、`.runtimeconfig.json`、`.deps.json`；
- C# 运行时代码不依赖 `powershell.exe` / `pwsh.exe`；
- 生成 `CodexDoctorV9.exe.sha256`；
- 如果 `v9.0.0` Release 已存在，发布工作流明确跳过，不覆盖任何既有资产。
